# RPGraph Continuation Handoff — 2026-09-11 (M6 voice complete, next family up)

## Start Here

This session finished M5 (social post/comment, see the prior handoff `docs/handoff/2026-09-11-m6-next-chat.md`, now historical) and then M6's first family (voice messages). **Resume by picking the next S10 family — notes, assistant chats, or choices/UI controls — and implementing it with the exact pattern established across M5/M6.** Leave banking for last; see "Why banking is different" below before attempting it.

## Workspace And Safety

- Working directory: `C:\Users\hen\Documents\Claude\RPGraph_sloppified`
- Branch: `codex/code-quality-cleanup`. **Nothing has been committed this entire project so far** — do not reset, stash-drop, or discard any dirty work.
- Runtime: shell default Node is below the project's `>=24` requirement. Use the bundled Node 24 for all test/build/lint/script commands:

```bash
node_path="C:\Users\hen\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
"$node_path" node_modules/vitest/vitest.mjs run
"$node_path" node_modules/typescript/bin/tsc -b --pretty false
"$node_path" node_modules/eslint/bin/eslint.js <files>
node_dir=$(dirname "$node_path"); PATH="$node_dir:$PATH" npm run build
node_dir=$(dirname "$node_path"); PATH="$node_dir:$PATH" npx playwright test <spec>
```

- A known, pre-existing Electron test flake: occasionally the *first* test in a Playwright run fails with `Execution context was destroyed, most likely because of a navigation`. Re-running the same spec in isolation always passes (confirmed again this session on `actionBaseline.spec.ts`) — don't chase it.
- **Important**: after changing `src/actions/schema.ts`, always run `npm run actions:schema` (under bundled Node 24) to regenerate `electron/providers/actionReply.schema.json`, or `electron/providers/responseContracts.test.ts` fails with a stale-artifact mismatch.

## What Changed This Session

Read the full detail in the progress log before touching related code:
- [2026-09-11: Social Post/Comment — Live (M5, Complete)](../review/action-runtime-progress.md#2026-09-11-social-postcomment--live-roadmap-m5-complete)
- [2026-09-11: Voice Messages — Live (M6, First Family)](../review/action-runtime-progress.md#2026-09-11-voice-messages--live-roadmap-m6-first-family)

**The voice slice's key lesson, worth internalizing before picking the next family**: don't trust the recipe inventory's speculative design for an unimplemented family at face value. `voice.message`'s original entry assumed a `voiceProviderId` locked argument and a `voiceDirection` creative field — neither corresponds to anything real. Investigation (via a read-only Explore pass over `appendPhoneMessage`, `MessageRecord`, `useDialogueVoice.ts`, and the two voice-bubble rendering gates in `PhonePanel.tsx`/`ChatConversationPanel.tsx`) revealed voice synthesis is fully lazy/client-side, so "send a voice message" is *exactly* a `whatsup.message` plus one boolean flag — no new canonical action, no new executor, no new staged-adapter branch needed; it fell through to the existing generic recipe-definition branch automatically. **Before implementing notes/assistant/choices, spend the same kind of investigation time confirming what the inventory's locked arguments/creative fields actually correspond to in the real codebase** — the previous-session design for any not-yet-implemented family may be wrong in the same way.

Summary of the actual voice-message change (all four points needed for a canonical-action extension, in case notes/assistant fit the same "extend an existing action" shape rather than needing a brand new one):
1. `contracts.ts`/`schema.ts`/`compileReply.ts`/`executionRegistry.ts`/`liveBridge.ts`: threaded an optional `isVoiceMessage: boolean` through `messenger.send` end to end.
2. `recipeInventory.ts`: corrected the entry's locked arguments/creative fields to match reality, promoted to `advertised: true`/`executor-backed`.
3. `stagedActionAdapter.ts`: extended the existing WhatsUp branch's recipe-id check by one, added the flag to the compiled intent.
4. `useGraphRun.ts`: **no changes needed** — confirmed by reading how the delivered message actually renders (gated on the real `MessageRecord`, not the embed-link pointer).
5. New Electron regression `test/e2e/voiceMessage.spec.ts`, asserting the autosave timeline entry's `phone.voiceMessage` field (nested — not top-level; see `src/data-management/timelineStore.ts:126-134` for the exact shape if adding a similar assertion).

## Verification Ledger (end of this session)

- **382 tests in 67 files pass**, `tsc -b`/ESLint clean, `npm run build` passes.
- **10/10 action-related Electron regressions pass together**: `voiceMessage.spec.ts` (2, new), `socialActions.spec.ts` (2), `stagedWorkflow.spec.ts` (2), `structuredActions.spec.ts` (2), `actionBaseline.spec.ts` (2).

## Next Step: Pick The Next S10 Family

Per `recipeInventory.ts`'s current inventory (`note.write`, `assistant.chat`, `choice.information`, `ui.control` are the remaining unadvertised, non-banking families), and the roadmap's stated order ("... notes/assistant/banking -> displays/controls ..."):

1. **Read the existing entry first** (`recipeInventory.ts`) and cross-check its `lockedArguments`/`creativeFields`/`missing` list against the real legacy implementation (`src/chat/phoneAppsSessions.ts` for notes/assistant-chat parsing, `src/chat/outputActions.ts` for choices/info-boxes/progress-bars/controls) before assuming the design is correct — see the voice lesson above.
2. **`note.write` and `assistant.chat`** are both `initiators: ['direct-user']` only in the current inventory (not model-authored) — confirm whether that's still accurate, since a `direct-user`-only family may hit the same "no existing direct-staged-action entry point" problem flagged for banking (see below). If so, either family might need the same entry-point work as banking, in which case it's not actually an easier next slice than banking despite being earlier in the stated order — verify this before committing to one.
3. **`choice.information`/`ui.control`** are `model`/`direct-user` respectively; check whether either has a clean, executor-backed shape (a receipt with no real side effect might not need a full canonical action at all — closer to `narration.speech`'s composer-only pattern than to `messenger.send`'s executor-backed pattern).

## Why Banking Is Different (do it last, or plan deliberately)

`bank.transfer`'s `initiators` is `['direct-user']` only — it is never something the model authors inline in a chat reply, unlike every family implemented so far (messenger, social, voice, narration are all `model`-initiated, or the model is at least one accepted initiator). The current "direct action" path (triggered by explicit UI interaction, e.g. clicking a "Send $50" button) runs through the legacy `directActionOnly`/graph-evaluation pipeline, **not** the new shared action executor (`src/actions/runtime.ts`/`liveBridge.ts`) that `actions-v1`/`staged-v1` both use. Wiring a first "direct staged/structured action" entry point into the shared executor is genuinely separate, larger work — not a same-pattern slice like social/voice were. If `note.write`/`assistant.chat` turn out to have the same direct-user-only constraint (see Next Step #2), that entry-point work may need to happen before *any* of banking/notes/assistant can go live, which would make it the actual next priority rather than something to defer.

## Suggested Opening Request For The New Chat

> Read `C:\Users\hen\Documents\Claude\RPGraph_sloppified\docs\handoff\2026-09-11-m7-next-chat.md` and its linked progress-log entries. Continue with the next S10 family — investigate whether notes/assistant/choices/controls need the same "direct-staged-action entry point" work flagged for banking before picking one, then implement it end to end (canonical action or extension, recipe promotion, staged adapter, tests, Electron regression) following the exact pattern M5/M6 established. Preserve all existing dirty work; nothing has been committed this whole project so far.
