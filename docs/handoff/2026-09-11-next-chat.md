# RPGraph Continuation Handoff — 2026-09-11

## Start Here

This is an end-of-session checkpoint, written because the previous session's context was running low. **Resume by finishing M5 (social.post/social.comment) at the `useGraphRun.ts`/`recipeInventory.ts` layer — the compiler/executor foundation is done and tested; only the live wiring remains.** Do not restart the review, redo the roadmap, or re-derive the architecture — read this handoff and the linked docs, then implement the next bounded step.

This session continued directly from an earlier handoff (`docs/handoff/2026-09-09-next-chat.md`, now historical) and completed, in order: M1 (staged workflow live context/plan-call wiring), M2 (narration recipe), M3 (S7 composer + first real committed staged turn), M4 (H7 effect journal, first slice), and M5 partial (social post/comment compiler+executor foundation). All of M1-M4 are complete, tested and documented. M5 is intentionally stopped mid-way at a clean, fully-compiling, fully-tested boundary — see "Current State" below.

## Workspace And Safety

- Working directory: `C:\Users\hen\Documents\Claude\RPGraph_sloppified`
- Branch: `codex/code-quality-cleanup`. HEAD unchanged from before this session: `0710e03492d8a87c5697f6617dca16e5c59ba477` ("refactor: centralize phone mood status"). **Nothing has been committed this entire session** — nothing in M1-M5 is in git history yet. `git status --short` shows ~83 modified/untracked files (the accumulated work from this session plus everything already dirty before it started). Do not reset, stash-drop, or discard any of this.
- This is the SAME logical worktree the 2026-09-09 handoff described (that doc's path was `C:\Users\hen\Documents\ChatGPT\rpgraph\.worktrees\code-quality-cleanup`; this session ran from `C:\Users\hen\Documents\Claude\RPGraph_sloppified` — same branch/history, just a different local checkout location). Don't be alarmed by the path mismatch; it isn't a different project state.
- Shell: PowerShell/Bash both available. Runtime: shell default Node is 22.22.0, below the project's `>=24` requirement. Use the bundled Node 24.19.0 for all test/build/lint/script commands:

```bash
node_path="C:\Users\hen\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
"$node_path" node_modules/vitest/vitest.mjs run
"$node_path" node_modules/typescript/bin/tsc -b --pretty false
"$node_path" node_modules/eslint/bin/eslint.js <files>
node_dir=$(dirname "$node_path"); PATH="$node_dir:$PATH" npm run build
node_dir=$(dirname "$node_path"); PATH="$node_dir:$PATH" npx playwright test <spec>
```

- A known, pre-existing Electron test flake: occasionally the *first* test in a Playwright run fails with `Execution context was destroyed, most likely because of a navigation` on an early `electronApp.evaluate` call. Re-running the same spec in isolation always passes. This happened at least twice this session, unrelated to any code change — don't chase it, just re-run once before concluding something regressed.

## Required Reading

1. This handoff (you're reading it).
2. [Action runtime progress log](../review/action-runtime-progress.md) — newest entries first: "Social Post/Comment — Compiler/Executor Foundation Only (Roadmap M5, Partial)", then H7 effect journal (M4), then S7 composer (M3), then S5 narration (M2), then S6 live wiring (M1). Each entry has full file-by-file detail and verification numbers — this handoff summarizes, that log is authoritative.
3. [Parent cleanup roadmap](../superpowers/plans/2026-09-06-code-quality-cleanup.md) — consolidated state near the top, and the H7 section (~line 191) for durable-lifecycle status.
4. [Staged workflow roadmap](../superpowers/plans/2026-09-09-staged-workflow.md) — S1-S7 checkboxes, all now `[x]` with dated update notes.

There is no separate local plan file from this session (it lived at `C:\Users\hen\.claude\plans\witty-snacking-puffin.md`, outside this repo, and won't be available in a new session/chat — this handoff supersedes it as the durable record).

## Current State: What Actually Works Today

**Staged Workflow v1** (`actionProtocol: 'staged-v1'` on the RP Output node, fully opt-in) runs a real, live, end-to-end turn: captures live context, calls a real planner LLM, compiles the plan, runs real content generation, runs real effects (WhatsUp sends, image generation attempts) through the *same* shared executor Structured v1 uses, composes the result deterministically, and commits it into chat/turn history/autosave through the exact same primitives Structured v1 already relies on. Verified live in `test/e2e/stagedWorkflow.spec.ts` (2 cases). Known limits: only `narration.speech`, `whatsup.message`, `whatsup.message-with-artifact`, `image.generate` are advertised recipes; image generation fails structurally in staged mode (no `imageRunner` outside `executeGraph`, which staged mode never calls — this is a real, documented gap, not a bug); only one "primary" character's context reaches the planner (true multi-actor scenes need more work); no continuation planning yet.

**H7 durable effect journal** (first slice) now durably records every real effect (WhatsUp send / image generation) attempted through the shared executor — both `actions-v1` and `staged-v1` — immediately before the effect runs and immediately after, so a crash between "effect fired" and "turn committed" can no longer silently lose all evidence of it. Cleared once the enclosing turn actually commits. Verified with a real Electron test that triggers a genuine failure (no mocking of the journal itself) and confirms the durable record survives. **Explicitly not implemented**: automatic reconciliation, safe retry-without-regenerating, action-preserving regeneration — this slice only stops silent evidence loss.

**Social post/comment compiler+executor foundation** (this session's last piece, incomplete): `src/actions/{contracts,schema,compileReply,executionRegistry,liveBridge}.ts` all now fully support `social.post` and `social.comment` as canonical actions — compile-time validation (author must be a real character with a configured Fotogram/OnlyFriends account), a real `ExecutionDefinition` for each, and `liveBridge.ts`'s catalog now computes `fotogram.social`/`onlyfriends.social` capabilities from actual character data. This is proven end-to-end in `src/actions/liveBridge.test.ts` (compile → execute → real adapter call, plus a denial case). **But nothing in the live app can trigger this yet** — see "Next Step" below.

## Next Step: Finish M5 (Social Post/Comment)

Three things remain, in this order:

1. **`useGraphRun.ts`'s live `postSocial`/`commentOnSocial` implementation.** Currently stubbed at the `createLiveActionBridge({...})` call site (~line 1560, search for `TODO(M5 follow-on)`) to throw a clear "not yet available" error — this is intentional and safe, not a bug. Replace it with a real implementation:
   - For `postSocial`: construct a `SocialPostRecord` the same way the existing legacy path does in `src/components/phone-social/PhoneSocialFeedScreen.tsx`'s `submitPost` (~line 1003-1067) — `postId` via `nextSocialPostId` (`src/chat/socialMedia.ts:262-280`), then commit via `appendMessage({role:'output', socialPost: record, ...})` (need to confirm the exact `appendMessage` field name/shape for attaching a *new* post — trace how `message.socialPost` is read elsewhere, e.g. in `src/app/socialOutputCommits.ts:19`, to get the shape right). This session's implementation only needs to support **text-only posts** (no image) — matches what `SocialPostAction`/`social.post`'s schema already declares (no attachment field yet).
   - For `commentOnSocial`: reuse the existing pure `buildSocialCommentCommit` (`src/app/socialOutputCommits.ts:16-31`) — build the `incoming` shape it expects from the action's `{app, authorId→character name, postId, text}`, then apply the returned commit via `appendMessage({role:'output', originalText: commit.historyText, socialReactions: commit.reactions, ...})`, mirroring exactly how `useGraphRun.ts:2631-2639` already does this for the legacy path (read that block for the exact pattern before writing new code — do not diverge from it, reuse it).
2. **Handle the new result types in the structured-actions commit block.** `useGraphRun.ts`'s `if (actionBridge) { ... }` block (the one already handling `outcome.result?.type === 'messenger.sent'` and `'image.generated'`, ~line 1670-1690) needs new branches for `'social.posted'` and `'social.commented'` — likely just an `appendMessage` call rendering the post/comment into the visible reply, analogous to the existing branches. Staged mode's `runLiveStagedTurn.ts`/`composeReply.ts` should need **no changes** — `composeReply.ts` already handles any `receipt`-kind value generically (it doesn't branch on the action type), so once `social.post`/`social.comment` are advertised recipes (next point), staged beats referencing them should compose automatically. Verify this assumption with a real test rather than assuming it holds.
3. **Advertise the recipes.** `src/staged-workflow/recipeInventory.ts` already has `social.post` and `social.comment` inventoried (`advertised: false`, `implementation.status: 'pending-adapter'`/`'compatibility-only'`). Flip them to `advertised: true`, `implementation: {status:'executor-backed', executor:'shared-action-runtime', action:'social.post'|'social.comment'}`, add them to `whitelistedActionRecipeIds` in `stagedRecipeDefinitions()`, and add their `RecipeDefinition` (steps: one `generate-content` draft stage for caption/comment text, one `execute-action` stage — follow the exact `whatsup.message` pattern already there). Then `stagedActionAdapter.ts` needs a matching stage/recipe branch (follow the `whatsup.message` branch there exactly) to translate the compiled stage into the `social.post`/`social.comment` action-v1 envelope.

**Tests needed**: focused unit tests for each file touched above (follow the existing test-per-file convention); one new Electron regression proving a real committed social post and a real committed comment, for both `actions-v1` and `staged-v1`, modeled directly on `test/e2e/structuredActions.spec.ts` and `test/e2e/stagedWorkflow.spec.ts`. Also worth adding (flagged as a gap in the progress log): dedicated `schema.test.ts`/`compileReply.test.ts` conformance cases for `social.post`/`social.comment`, matching `messenger.send`'s existing coverage style — the current coverage is only the one end-to-end `liveBridge.test.ts` case.

**After M5 is fully live**: the roadmap's next milestones are M6 (voice/notes/assistant/choices-controls family rollout, same pattern again), M7 (continuation planning + first full mixed acceptance scene), M8 (S9 editor/migration), then M9-M14 (H6 remaining, whole-app cleanup Tasks 10-16, final gates/merge). Full table in the parent cleanup roadmap's "Milestone Roadmap"-equivalent section — actually that table lived only in this session's local (non-repo) plan file; reconstruct it from the roadmap docs' checkboxes if needed, or just continue in the order S10 family rollout → continuation planning → acceptance scene → editor → whole-app cleanup, which is what every roadmap doc already independently states.

## Verification Ledger

- M1-M4: 360 tests in 67 files, `tsc -b`/ESLint clean, `npm run build` passes, 6/6 Electron regressions (`structuredActions.spec.ts` ×2, `actionBaseline.spec.ts` ×2, `stagedWorkflow.spec.ts` ×2).
- M5 (partial, this session's last checkpoint): 361 tests in 67 files (net +1 vitest test-count delta was actually several new cases across files, offset by none removed), `tsc -b`/ESLint clean, `npm run build` passes, same 6/6 Electron regressions still pass (re-verified after the social-action changes, including the pre-existing flake re-run once).
- **Important**: after changing `src/actions/schema.ts`, always run `npm run actions:schema` (under bundled Node 24) to regenerate `electron/providers/actionReply.schema.json`, or `electron/providers/responseContracts.test.ts` will fail with a stale-artifact mismatch. This session hit that exact failure once and fixed it — don't repeat the miss.

## Suggested Opening Request For The New Chat

> Read `C:\Users\hen\Documents\Claude\RPGraph_sloppified\docs\handoff\2026-09-11-next-chat.md` and its required reading. Continue from "Next Step: Finish M5" — implement the real `postSocial`/`commentOnSocial` wiring in `useGraphRun.ts`, the new result-type handling, and advertise the recipes, in that order, with focused tests at each step and a full-suite/build/Electron-regression check at the end. Preserve all existing dirty work; nothing has been committed this whole project so far.
