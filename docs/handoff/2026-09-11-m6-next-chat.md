# RPGraph Continuation Handoff — 2026-09-11 (M5 complete, M6 next)

## Start Here

This session finished M5 (social post/comment) end to end, per the previous handoff (`docs/handoff/2026-09-11-next-chat.md`, now historical — superseded by this doc). **Resume by starting M6: one bounded family from the S10 rollout order (banking, voice, notes, assistant, choices/controls — pick banking or voice first, they're the next items in that stated order).** Do not restart the review or re-derive the architecture — read this handoff and the linked docs, then implement the next bounded family using the exact pattern M5 just established.

## Workspace And Safety

- Working directory: `C:\Users\hen\Documents\Claude\RPGraph_sloppified`
- Branch: `codex/code-quality-cleanup`. **Nothing has been committed this entire project so far** — do not reset, stash-drop, or discard any dirty work. `git status --short` will show many modified/untracked files; that is expected and pre-existing.
- Runtime: shell default Node is below the project's `>=24` requirement. Use the bundled Node 24 for all test/build/lint/script commands:

```bash
node_path="C:\Users\hen\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
"$node_path" node_modules/vitest/vitest.mjs run
"$node_path" node_modules/typescript/bin/tsc -b --pretty false
"$node_path" node_modules/eslint/bin/eslint.js <files>
node_dir=$(dirname "$node_path"); PATH="$node_dir:$PATH" npm run build
node_dir=$(dirname "$node_path"); PATH="$node_dir:$PATH" npx playwright test <spec>
```

- A known, pre-existing Electron test flake: occasionally the *first* test in a Playwright run fails with `Execution context was destroyed, most likely because of a navigation`. Re-running the same spec in isolation always passes; don't chase it.

## What Changed This Session (M5, now complete)

Read the full detail in [the progress log's newest entry](../review/action-runtime-progress.md#2026-09-11-social-postcomment--live-roadmap-m5-complete) before touching related code. Summary:

- `useGraphRun.ts`'s `postSocial`/`commentOnSocial` live-bridge callbacks now do real work (previously stubbed to a thrown error): `postSocial` builds a `SocialPostRecord` via `nextSocialPostId`/`socialHandleForCharacter` and commits it with `appendMessage({..., socialPost: record})`; `commentOnSocial` reuses the existing `buildSocialCommentCommit` builder. Both run inside the shared executor's `execute()` loop (strictly sequential), so a same-reply comment can reference the post an earlier operation just created.
- Structured v1's and Staged v1's commit blocks each gained a no-op branch for the new result types (`social.posted`/`social.commented`, and a `social:`-prefixed staged receipt id) — no separate render step needed, since the adapter already appended the real message.
- `stagedActionAdapter.ts` gained `social.post`/`social.comment` stage branches (mirrors `whatsup.message`'s pattern exactly).
- `recipeInventory.ts`: `social.post`/`social.comment` promoted to `advertised: true`/`executor-backed`; their locked-argument shapes were corrected in the process (a stale, never-live design had locked `accountId`/`actorId`, which don't exist on the canonical actions — now only `app` (post) and `app`+`postId` (comment) are locked, everything else derives from the actor or is generated).
- Fixed a real bug in `compileReply.ts` found while adding schema conformance tests: `social.post`'s `caption` field compiled with `allowEmpty: true` (a stray argument), so blank captions passed compilation despite `social.post` being a text-only-post action. Removed the stray argument.
- New Electron regression [test/e2e/socialActions.spec.ts](../../test/e2e/socialActions.spec.ts): one `actions-v1` case (post then comment on it, same reply) and one `staged-v1` case (post via a compiled plan). Both assert against the real `SocialPostCard` widget UI — a committed post renders as a phone-style card button, not plain chat text (the plain history text exists only in the message's `originalText`/autosave, not the visible timeline) — and both assert the H7 journal ends empty after commit.

## Verification Ledger (end of this session)

- **375 tests in 67 files pass**, `tsc -b`/ESLint clean on all touched files, `npm run build` passes.
- **8/8 action-related Electron regressions pass together**: `socialActions.spec.ts` (2, new), `stagedWorkflow.spec.ts` (2), `structuredActions.spec.ts` (2), `actionBaseline.spec.ts` (2).
- Reminder that still applies: after any `src/actions/schema.ts` change, run `npm run actions:schema` (under bundled Node 24) to regenerate `electron/providers/actionReply.schema.json`, or `electron/providers/responseContracts.test.ts` fails with a stale-artifact mismatch. Not needed this session (schema.ts itself wasn't touched — only compileReply.ts/recipeInventory.ts/stagedActionAdapter.ts/useGraphRun.ts), but will matter again once M6 needs new/changed action definitions.

## Next Step: M6 — Next S10 Family

Per the roadmap's stated order (`docs/superpowers/plans/2026-09-09-staged-workflow.md` line ~41: "S10 Rollout: ... narration/messages/images -> social/voice -> notes/assistant/banking -> displays/controls ..."), the next unadvertised families are **voice** and **banking**. Pick one and follow the exact M5 pattern:

1. **Inventory check**: `recipeInventory.ts` already documents `voice.message` and `bank.transfer` as `compatibility-only`/`direct-app-action` with a `missing` list — read those entries first; they note what's actually absent (e.g. voice needs "typed voice artifact receipt" and "shared staged executor adapter"; banking needs "authoritative account IDs", "balance refresh at effect boundary", "staged banking receipt").
2. **Canonical action**: does `src/actions/contracts.ts`/`schema.ts`/`compileReply.ts`/`executionRegistry.ts` already have a canonical action for this family? For social, `messenger.send`'s pattern was cloned; for banking specifically, note the prior session's explicit call-out ("`bank.transfer` was deliberately dropped from M5... there is no existing 'direct staged/structured action' entry point — wiring it in is genuinely separate, larger work" — see the M5-partial progress log entry). Banking may need a real direct-action entry point built first, which is a bigger lift than voice; consider starting with **voice** instead if a smaller first M6 slice is preferred.
3. **liveBridge.ts**: extend `Options`/adapters/catalog capabilities the same way `postSocial`/`commentOnSocial`/`fotogram.social`/`onlyfriends.social` were added for social.
4. **useGraphRun.ts**: real adapter implementation + result-type commit-block branches (structured and staged), following exactly how social's were just added.
5. **recipeInventory.ts + stagedActionAdapter.ts**: advertise + map, following the `social.post`/`social.comment` branches just added as the direct template.
6. **Tests**: unit tests per touched file (schema/compileReply/executionRegistry/liveBridge/recipeInventory/stagedActionAdapter), then a new Electron regression modeled directly on `test/e2e/socialActions.spec.ts`.

## Suggested Opening Request For The New Chat

> Read `C:\Users\hen\Documents\Claude\RPGraph_sloppified\docs\handoff\2026-09-11-m6-next-chat.md` and its linked progress-log entry. Continue with M6: implement one bounded S10 family (voice or banking — your call, but read the "Next Step" section's guidance on relative scope first) using the exact pattern M5 (social post/comment) just established. Preserve all existing dirty work; nothing has been committed this whole project so far.
